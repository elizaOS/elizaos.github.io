# elizaos-plugins/plugin-openrouter Daily Update (Nov 3, 2025)
## OVERVIEW 
Today's development focused on expanding the plugin's capabilities by integrating support for text embedding models, enhancing its functionality within the ElizaOS framework.

## KEY TECHNICAL DEVELOPMENTS

### Text Embedding Model Integration
- Added support for `TEXT_EMBEDDING` models, including a new handler and configuration options. ([#17](https://github.com/elizaos-plugins/plugin-openrouter/pull/17))
- Implemented `handleTextEmbedding` in `models/embedding.ts` to process text embedding requests. ([#17](https://github.com/elizaos-plugins/plugin-openrouter/pull/17))
- Registered the `TEXT_EMBEDDING` model type within the plugin definition. ([#17](https://github.com/elizaos-plugins/plugin-openrouter/pull/17))
- Introduced configuration support for `OPENROUTER_EMBEDDING_` settings. ([#17](https://github.com/elizaos-plugins/plugin-openrouter/pull/17))

## NEWLY OPENED PULL REQUESTS
- No new pull requests were opened today.

## CLOSED ISSUES
- No issues were closed today.

## NEW ISSUES
- No new issues were reported today.

## ACTIVE ISSUES
- No active issues with more than 3 comments.